"""All-In-One (`allin1`) feature extraction — experiment, not pipeline code.

See README.md. Nothing in `src/` may import from this package (experiments are a sandbox; `src/` never imports from them).
"""
