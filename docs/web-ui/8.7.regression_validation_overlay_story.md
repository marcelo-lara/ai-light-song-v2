# EPIC 8 - Story 8.7

## Regression Validation Overlay

## Goal

Surface validation and drift information directly on top of the artifact viewer.

## Inputs

- `data/analysis/<Song - Artist>/artifacts/validation/phase_1_report.json`
- `data/analysis/<Song - Artist>/artifacts/event_inference/timeline_index.json`
- `data/analysis/<Song - Artist>/song_event_timeline.json`

## Acceptance Criteria

- Validation overlays are derived from existing reports and timing artifacts.
- The debugger can highlight mismatches or drift without modifying artifacts.
- Any snapshot or export flow remains out of generated-data folders unless a future contract says otherwise.

## UI v2 component map (plan item 10)

The React + TypeScript rebuild (`docs/web-ui/ui-rebuild/`) implements this story in:

- `ui/src/App.tsx` — the `validation` lane currently renders an empty-state stub (see D6 in the implementation plan; wiring deferred to item 11 parity).
- `ui/src/timeline/laneRenderers.ts` — `VALIDATION_MARK_COLORS` in place for when it is wired.
